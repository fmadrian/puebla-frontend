const API_VERSION = "1";
const SERVER = "http://localhost:5006";

export const environment = {
    API_URL: `${SERVER}/api/v${API_VERSION}`,
    RECOVERY_PASSWORD_EMAIL_ADDRESS: '',
    CLIENT_ADDRESS: 'http://localhost:4200',
    WEBAPP_VERSION: "0.1.0"
};
